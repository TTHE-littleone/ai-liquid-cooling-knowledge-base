import { sources } from "@/lib/data"; import { PageIntro,SiteFooter,SiteHeader } from "@/components/SiteChrome";
export default function Sources(){const p0=sources.filter(x=>x.priority==="P0").length;return <><SiteHeader/><main className="subpage"><PageIntro kicker="SOURCE COVERAGE" title="来源覆盖与漏检审计" description="“不遗漏”不是一句承诺，而是来源清单、采集状态、失效记录和覆盖率指标。" meta={`${sources.length}个首发来源组 · ${p0}个P0核心来源`}/><div className="coverage-cards"><article><span>核心来源在线率</span><strong>目标 ≥95%</strong><p>P0来源失败时在周报显著提醒</p></article><article><span>新闻入库延迟</span><strong>目标 ≤7天</strong><p>重大事项目标为当天预警</p></article><article><span>证据可追溯</span><strong>目标 100%</strong><p>结论必须带来源与采集日期</p></article></div><div className="table-wrap"><table><thead><tr><th>来源</th><th>类型</th><th>区域</th><th>优先级</th><th>采集方法</th><th>入口</th></tr></thead><tbody>{sources.map(x=><tr key={`${x.name}-${x.type}`}><td><b>{x.name}</b></td><td>{x.type}</td><td>{x.region}</td><td><span className={x.priority==="P0"?"priority p0":"priority"}>{x.priority}</span></td><td>{x.method}</td><td><a href={x.url} target="_blank" rel="noreferrer">访问 ↗</a></td></tr>)}</tbody></table></div></main><SiteFooter/></>}
