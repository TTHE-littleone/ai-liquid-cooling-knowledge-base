import { weeklySignals } from "@/lib/data"; import { PageIntro,SiteFooter,SiteHeader } from "@/components/SiteChrome";
export default function Weekly(){return <><SiteHeader/><main className="subpage"><PageIntro kicker="WEEKLY INTELLIGENCE" title="本周变化，而不是重复新闻" description="每天08:00和20:00采集；每周一07:00统一发布。重大政策、强制标准、财报和项目进入当天预警。" meta="网页 + Markdown + 邮件"/><div className="weekly-list">{weeklySignals.map(x=><article key={x.title}><div><time>{x.date}</time><span>{x.level}</span></div><h2>{x.title}</h2><p>{x.summary}</p><a href={x.source} target="_blank" rel="noreferrer">原始来源 ↗</a></article>)}</div><div className="method-note"><b>自动摘要边界</b><p>零成本版本不调用付费大模型。系统先用关键词、来源等级、日期与去重规则生成结构化摘要；低可信内容进入待核验区，不直接写成结论。</p></div></main><SiteFooter/></>}
